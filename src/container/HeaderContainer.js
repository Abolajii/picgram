import React from 'react';
import { Header } from '../components';

const HeaderContainer = () => {
	return (
		<Header>
			<Header.Inner>
				<Header.Title>Firegram</Header.Title>
			</Header.Inner>
		</Header>
	);
};

export default HeaderContainer;
